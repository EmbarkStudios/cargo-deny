[package]
name = "safe-wrapper"
version = "0.1.0"
authors = []
edition = "2018"
license = "MIT"

[dependencies]
dangerous-dep = { version = "0.1.0", path = "../dangerous-dep", features = [
    "not-dangerous-at-all",
] }
