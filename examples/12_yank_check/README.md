# 02_yank_check

This example shows what happens when a crate version in the graph has been yanked from its source registry.

## Description

To run this example you need to config for the remote registries, so you need to run `(cd examples/12_yank_check && cargo run --manifest-path ../../Cargo.toml -- check advisories)`.
